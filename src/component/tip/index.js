import React, { Component } from "react";
import "./index.less";

export default class Tip extends Component {
  render() {
    return (
      <div className="tip">
        <span>
          There are questions remain, We'll search for the answers together. But
          one thing we known for sure,the future is not set!
        </span>
      </div>
    );
  }
}
